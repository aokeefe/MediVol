from catalog.models import Category, BoxName, Item
from inventory.models import Box, Contents
from orders.models import Order, OrderBox

model_list = [Category, BoxName, Item, Box, Contents]#, Order, OrderBox]